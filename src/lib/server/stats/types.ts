/**
 * Server Statistics Types
 *
 * Re-exports from the shared location for backward compatibility.
 * All types are now in $lib/stats/types.ts for client-safe access.
 *
 * @module server/stats/types
 */

// Re-export everything from shared types
export * from '$lib/stats/types';
