# Test environment configuration
# This file is loaded by bun test via --env-file flag

# Use in-memory SQLite database for tests
# This prevents tests from corrupting the production database
DATABASE_PATH=:memory:

# Mark environment as test
NODE_ENV=test
