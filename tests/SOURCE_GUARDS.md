# Retained Bun source guard registry

These tests intentionally read source files with `Bun.file(...).text()`. They
are retained only where Bun-only unit coverage cannot directly observe a
component/hook wiring invariant without a DOM/browser runner or invasive source
extraction.

| Guard file | Invariant label | Owning domain | Why behavior coverage is insufficient | Removal condition |
| --- | --- | --- | --- | --- |
| `tests/unit/admin/slides-actions.test.ts` | `admin-slides-enhance-order` | Admin/slides UI | The invariant is Svelte form-enhance callback ordering in `+page.svelte`; Bun has no DOM runner in this suite to exercise the editor failure path. | Replace with a browser/component test that submits the create/update custom slide form and proves failure preserves editor content while success closes after `update()`. |
| `tests/unit/auth/core.test.ts` | `auth-returnTo-threading` | Auth routing | Pure validator behavior is covered, but the redirect page/server data threading spans SvelteKit route files that are not all callable as isolated Bun units. | Add route/component harness coverage for the redirect load/page flow where onboarding context wins and safe `returnTo` is used otherwise. |
| `tests/unit/security/basics.test.ts` | `hooks-anon-admin-returnTo` | Auth hooks | `authorizationHandle` is composed inside `hooks.server.ts` and not exported separately; source guard pins anonymous admin path encoding and non-admin dashboard redirect. | Export a behavior-neutral authorization helper or add hook-level request tests that can invoke this branch directly. |
| `tests/unit/security/basics.test.ts` | `onboarding-bootstrap-origin` | Onboarding hooks | The bootstrap banner call is an initialization side effect inside the hook chain; the negative origin argument is not externally observable in Bun unit tests. | Extract the banner invocation decision or add a hook harness that spies on the banner call without importing request-derived origin. |
| `tests/unit/onboarding/settings.test.ts` | `onboarding-settings-substeps` | Onboarding UI | The multi-substep footer/rendering behavior lives in a Svelte page and requires DOM interaction for true behavior coverage. | Replace with component/browser tests that walk the settings substeps and assert submit button gating. |
| `tests/unit/plex-login.test.ts` | `plex-login-browser-safe-fields` | Auth client/API | Existing unit tests cover client helpers, but browser-safe response-shape constraints span auth endpoint and redirect load source without a direct exported schema. | Export a response DTO/schema or add route tests that assert completed auth payloads omit Plex tokens/private account fields. |
| `tests/unit/security/basics.test.ts` | `admin-guard-hook-wiring` | Security/auth hooks | Guard helpers are tested behaviorally; the hook's use of the helper and redirect target are not separately exported. | Add hook-level tests or an exported authorization decision helper covering admin route probes. |
| `tests/unit/security/basics.test.ts` | `plex-login-no-token-postMessage` | Security/auth client | This is a negative security invariant over absent popup/postMessage logging code; Bun unit tests cannot observe absent future branches without executing browser code. | Replace with browser/client tests that exercise the popup polling path and assert no token payload is posted or logged. |
| `tests/unit/sharing/presets.test.ts` | `privacy-preset-not-persisted` | Sharing/privacy UI/actions | Pure preset matching is covered, but the negative persisted/submitted field invariant spans Svelte forms and server action schemas. | Add action/form behavior tests proving preset selection only maps to persisted privacy fields and no `preset` form key is accepted. |
| `tests/unit/admin/dogfood-ui-invariants.test.ts` | `dogfood-ui-invariants` | Admin/onboarding settings UI | DOM-attribute invariants from the 2026-06-07 dogfood (Base URL `maxlength=512` (ISSUE-008), model placeholder default (ISSUE-003), Plex ENV pill/`disabled` keyed on `isLocked` (ISSUE-001)) live in Svelte templates; the Bun suite has no DOM runner to read rendered attributes. | Replace with component/browser tests that render the connections + onboarding settings forms and assert the input `maxLength`, placeholder text, and that an env-locked field renders the ENV pill and a disabled input. |
| `tests/unit/plex-login.test.ts` | `landing-cta-anchor-onclick` | Auth client/landing UI | The invariant is that the no-JS fallback `<a href="/auth/plex">` CTA on the landing page carries an `onclick` handler that calls `handlePlexLogin` and `preventDefault`, preventing Cmd+Click / middle-click from opening an orphan tab (ISSUE-009). This spans a Svelte template attribute; Bun has no DOM runner to assert click-handler wiring. | Replace with a browser/component test that fires click, Cmd+Click, and middle-click on the CTA and asserts only one navigation/popup fires per interaction. |
| `tests/unit/security/no-client-token-logging.test.ts` | `no-client-token-console-logging` | Security/auth client | DF-07: a negative invariant that no `console.log/debug/info` of Plex API response bodies exists in `src/lib/client/**`; absent code cannot be observed behaviorally in a Bun unit without scanning source. | Replace with a lint rule (custom Biome/ESLint guard) or a DevTools/browser assertion that no token payload reaches the console after OAuth. |
| `tests/unit/admin/sync-409-toast.test.ts` | `sync-409-toast-wiring` | Admin/sync UI | DF-08: the concurrent-sync 409 `form` result is surfaced via a global `$effect(() => handleFormToast(form))` in `admin/sync/+page.svelte`; the Bun suite has no DOM runner to observe the toast firing, so the guard pins the wiring in source. | Replace with a component/browser test that triggers a concurrent-sync 409 and asserts a "sync already running" toast renders. |
| `tests/unit/admin/security-diagnostic-toast.test.ts` | `diagnostic-toast-user-initiated` | Admin/security UI | ISSUE-007: the reverse-proxy diagnostic auto-runs on mount via a `$effect`; a success/error toast must fire ONLY on the user-initiated re-run, never the silent on-mount run. The Bun suite has no DOM runner to observe toasts, so the guard pins that `runDiagnostic` is gated on `userInitiated`, the re-run button passes `userInitiated: true`, and the on-mount call passes no args. | Replace with a component/browser test that loads the page (asserts no toast) and clicks "Re-run diagnostic" (asserts one toast). |
| `tests/unit/dashboard/settings-submit-guard.test.ts` | `dashboard-double-submit-guard` | Dashboard settings UI | ISSUE-003: double-submit prevention lives in Svelte enhance callbacks — a shared `isUpdating` gates share-mode + logo, a separate `isRegenerating` gates regenerate — with `disabled` button bindings. The Bun suite has no DOM runner to exercise rapid re-submits, so the guard pins the shared-flag wiring (and the intended share/logo coupling) in source. | Replace with a component/browser test that double-clicks each save button and asserts only one submission fires. |
| `tests/unit/dashboard/settings-toast-wiring.test.ts` | `dashboard-save-toast-wiring` | Dashboard settings UI | ISSUE-008: save results surface via a global `$effect` calling `handleFormToast(form)`; the Bun suite has no DOM runner to observe the toast, so the guard pins the `$effect` wiring in source (the action-payload half is a real action invocation, not a source scan). | Replace with a component/browser test that saves share-mode/logo and asserts a toast renders. |
